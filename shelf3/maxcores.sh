#!/bin/sh
# maxcores.sh
# REMEMBER: this sort of thing must be invoked with "source" or "."
#  in order to impact the current process environment.  

# Set NCORES for broker to be what the OS thinks we actually have
NCORES=$NUMBER_OF_PROCESSORS
# Not rocket science.  
