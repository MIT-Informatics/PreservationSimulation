TO MAKE MULTIPLE COPIES OF A COLLECTION

doc
collection
rep of collection
  number
  append to doc id

client has list of approved libraries

client sends out copy of entire collection to multiple libs
  client uses distribution params: client, value, n copies to quality m
  for a collection of some value, find the set of 

client maintains list of copies of each collection
  client sends collection to lib, adds lib to list of copies
  for each copy maintain list of docs
  if collection fails at lib, client removes from list of copies and resends
  if doc fails at lib, collection removes from its copy and resends

12client examines value, sends copies to n random of all libs at the required quality level also on the collections list of approved libraries










